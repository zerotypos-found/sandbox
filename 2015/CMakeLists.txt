file(GLOB subdir_files "*/*.c")
foreach(file ${subdir_files})
  get_filename_component(basename ${file} NAME_WE)
  set(target_name 2015_${basename})
  add_executable(${target_name} ${file})
  add_test(test.${target_name} ${target_name})
endforeach()
